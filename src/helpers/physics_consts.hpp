#pragma once

namespace Config::Physics
{
    inline constexpr double T_STEP = 0.1;            // Euler integration step (s)
    inline constexpr double G_FIELD_CONST = 9.80665; // Gravitational acceleration (m/s^2)
    inline constexpr double BALL_MASS = 0.057;       // In kg
}
